{
  "name": "<%= _.slugify( webappName ) %>",
  "version": "0.0.0",
  "dependencies": {
    <% if(jQuery) { %>
  		"jquery": "*",
    <% } if (jQueryMobile) { %>
      "jquery-mobile": "latest",
  	<% } if (angular) { %>
  		"angular": "*",
  	<% } if (bootstrap) { %>
  		"bootstrap": "*",
  	<% } if (parallax) { %>
  		"parallax": "*"
  	<% } %>
  }
}
